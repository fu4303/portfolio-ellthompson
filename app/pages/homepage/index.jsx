import React from 'react';
import { Wrapper, FlexCentered, Title, Text, LeadText, SmallText, P }  from 'styles';

const Homepage = () => (
    <Wrapper margin>
        <P>Hi there. I'm a web developer based in London.</P>
    </Wrapper>
);

export default Homepage;
